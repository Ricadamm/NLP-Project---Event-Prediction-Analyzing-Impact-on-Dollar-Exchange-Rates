[pytest]
testpaths = tests
addopts = -ra
# Tests import the repository-root `src` package. Only `python -m pytest` puts
# the working directory on sys.path, so a bare `pytest` (or a CI runner that
# calls the console script) failed collection with ModuleNotFoundError. Adding
# the rootdir explicitly makes both invocations equivalent.
pythonpath = .
