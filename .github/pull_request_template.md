## Summary

<!-- Why this change exists, in one or two sentences. -->

## What changed

<!-- Bullet the concrete changes. -->

## How verified

<!-- Commands run and their results, e.g. `pytest -q`, `ruff check .`. -->

## Checklist

- [ ] Tests pass (`pytest -q` / `python -m pytest -q`)
- [ ] Lint clean (`ruff check .`)
- [ ] `README.md` untouched, unless the change is intentional and explained above
- [ ] No `data/` outputs, `.venv/`, or cache directories committed
